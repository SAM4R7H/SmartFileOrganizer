#!/bin/bash

# Move to the directory where the script is located
cd "$(dirname "$0")"

# Run Java with the new driver and the flag to hide warnings
java --enable-native-access=ALL-UNNAMED -cp "bin:lib/sqlite-jdbc-3.42.0.0.jar" com.filesorter.Main "$@"
